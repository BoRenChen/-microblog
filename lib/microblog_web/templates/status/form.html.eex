<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <div class="form-group" hidden>
    <%= label f, :createBy, class: "control-label" %>
    <%= text_input f, :createBy, class: "form-control" ,value: @session_user.firstName %>
    <%= error_tag f, :createBy %>
  </div>

  <div class="form-group">
    <%= label f, :content, class: "control-label" %>
    <%= textarea f, :content, class: "form-control" %>
    <%= error_tag f, :content %>
  </div>

  <div class="form-group" hidden>
    <%= label f, :hashTag, class: "control-label" %>
    <%= text_input f, :hashTag, class: "form-control", value: @session_user.firstName%>
    <%= error_tag f, :hashTag %>
  </div>

  <div class="form-group" hidden>
    <%= label f, :atTag, class: "control-label" %>
    <%= text_input f, :atTag, class: "form-control", value: @session_user.firstName %>
    <%= error_tag f, :atTag %>
  </div>

  <div class="form-group" hidden>
    <%= label f, :like, class: "control-label" %>
    <%= number_input f, :like, step: "any", class: "form-control", value: 0 %>
    <%= error_tag f, :like %>
  </div>

  <div class="form-group" hidden>
    <%= label f, :user_id, class: "control-label" %>
    <%= number_input f, :user_id, step: "any", class: "form-control", value: @session_user.id%>
    <%= error_tag f, :user_id %>
  </div>

  <div class="form-group">
    <%= submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
